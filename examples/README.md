# Examples — Pi Interface V2

Sample code and reference designs.

## Structure

- `python/` — Python examples (typically run on the Pi)
- `arduino/` — Arduino sketches if applicable
- `embedded/` — bare-metal / RTOS examples

Each example folder includes its own README with wiring and usage.
